task: joint
model_config: configs/model/y26_focus_fourier.yaml
data_config: configs/data/joint_data.yaml
output_dir: runs/joint
epochs: 100
batch_size: 4
num_workers: 4
img_size: 640
learning_rate: 0.001
weight_decay: 0.0005
device: auto
save_every: 10
eval_every: 1
resume: null
seed: 42
loss:
  one2many_weight: 1.0
  one2one_weight: 1.0
  cls_weight: 1.0
  box_weight: 5.0
  iou_weight: 2.0
  contour_weight: 1.0
  boundary_weight: 0.25
